# Import a Math Library Partially [ for more description Enter help('math')]
from math import pow, sqrt
y = int(input('Enter a Number !'))
p = int(input('Enter a Power value !'))
x = pow(y, p)
# print a power as per user input basis
print(x)
